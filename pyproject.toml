[build-system]
requires = ["maturin>=1,<2"]
build-backend = "maturin"

[project]
name = "xquces"
version = "0.1.0"
requires-python = ">=3.10"

[tool.maturin]
features = ["pyo3/extension-module"]
python-source = "python"
module-name = "xquces._lib"